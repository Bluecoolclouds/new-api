Диагностика завершена, и тут важная новость. ☁️

Сначала главное: прод в порядке. Я собирала всё в /tmp и ничего не переключала — работает по-прежнему старый июньский бинарник, сервис не трогала.

Теперь суть проблемы. Свежий код форка сам по себе не собирается. Вчерашний коммит 1691af6 (это Replit Agent, "full_checkpoint") прописал в роутер 8 обработчиков, но сами файлы с этими функциями в коммит не попали. То есть на GitHub в Bluecoolclouds/new-api лежит незавершённое состояние — оно физически не компилируется:

• CreateLogCleanupSystemTask, ListSystemTasks, GetCurrentSystemTask, GetSystemTask (system-task роуты)
• GetAllFlowQuotaDates, GetUserFlowQuotaDates (flow-quota роуты)
• GetCodexChannelRateLimitResetCredits, ResetCodexChannelUsage (codex usage reset)

Роутер их вызывает — а в controller/ их нет, и в июньской версии тоже не было. Модельный слой под них частично есть (model/system_task.go, model/usedata_flow.go), но сами HTTP-хендлеры отсутствуют.

Два бага я уже нашла и починила по ходу (тоже в том же вчерашнем коммите): в model/apinet_payment.go было обращение к несуществующему полю topUp.Quota и неверная сигнатура вызова — поправила по образцу freekassa/старого heleket. Плюс твои локальные фиксы (подпись вебхука Heleket + USD-курс) вернула из stash без конфликтов. Но вот эти 8 недостающих хендлеров руками из воздуха не восстановить.

Корень один: на Replit файлы, скорее всего, есть, а в git их не закоммитили/не запушили. Отсюда варианты:

1. Самый чистый — ты в Replit-воркспейсе делаешь commit + push недостающих файлов (там должны лежать controller/system_task*.go, flow-quota хендлеры и codex-reset). Я перевыкачаю и дособеру. Это правильный фикс, без моих догадок.
2. Я сама допишу эти 8 хендлеров по модельному слою. Но честно: это уже разработка, а не поддержка, я буду угадывать контракты API, и это боевой платёжный сервис. Возьмусь только с твоим явным ОК.
3. Заглушу 8 строк в роутере, чтобы собралось прямо сейчас. Тогда обновление задеплоится, но без этих фич (админский log-cleanup, flow-quota вьюхи в админке, сброс codex-usage). Текущий фронт эти эндпоинты и так не дёргает, так что для пользователей разницы ноль. Обратимо.

Я бы шла по варианту 1 — глянь свой Replit, там наверняка эти файлы недокоммичены. Как скажешь — так и сделаю. Локальный клон сейчас на сломанном HEAD форка (+ мои фиксы), в любой момент откатываю на июньский тег pre-update-20260708_141858, если что.