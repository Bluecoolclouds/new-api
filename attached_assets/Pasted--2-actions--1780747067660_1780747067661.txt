Теперь запрос до сервера доходит — смотрю логи и код эндпоинта параллельно:


2 actions
Запрос до сервера доходит, но возвращает 401. Это или наш middleware блокирует маршрут до обработчика, или сам эндпоинт. Читаю оба места:


3 actions
Вижу что 401 приходит от нашего кода — это строка 290: APINET вернул 401 на ключ. Нужно проверить два варианта: либо ключ уже невалиден (тот что был в чате), либо Auth-middleware блокирует роут до его выполнения.


4 actions
Нашёл причину — /api/auth/login-apinet не в списке публичных маршрутов. Middleware блокирует его до обработчика с 401. Добавляю:


2 actions
Перезапускаю приложение чтобы изменение вступило в силу:


2 actions
Готово. Причина была простая — роут /api/auth/login-apinet не был добавлен в список публичных маршрутов, и Auth middleware отклонял запрос с 401 ещё до того, как он попадал в обработчик.

Попробуйте войти снова с новым ключом (старый sk-D7NjFg... из чата нужно отозвать на apinet.cloud и создать новый).


Checkpoint made
18 minutes ago

Worked for 57 seconds
Не удалось получить имя пользователя от APINET

17 minutes ago

Show less

Analyzing APINET username field (6 seconds)
APINET вернул успешный ответ, но поле username пустое — значит реальная схема ответа отличается от ожидаемой. Добавлю логирование и fallback на другие поля:


2 actions
Обновляю код — добавляю логирование ответа, поддержку data-обёртки и fallback на несколько полей:


4 actions
Приложение перезапущено. Попробуйте войти снова — теперь в логах будет видна реальная структура ответа APINET. Как только войдёте (или снова получите ошибку), дайте знать — сразу посмотрю логи и доправлю.


Checkpoint made
17 minutes ago

Worked for 43 seconds
проверь

13 minutes ago

3 actions
Вижу всё чётко в логах:

APINET profile fields: ['message', 'success']

APINET возвращает 200 OK даже для невалидных ключей, но тело {"success": false, "message": "..."}. Нужно проверять поле success перед обработкой профиля:


4 actions
Готово. Теперь картина полная:

Проблема была двойная:

Middleware блокировал роут — исправлено
APINET возвращает HTTP 200 даже при невалидном ключе, но в теле {"success": false, "message": "..."} — теперь это тоже проверяется
Теперь при неверном ключе вы увидите точное сообщение от APINET вместо «не удалось получить имя пользователя».

Следующий шаг: зайдите на apinet.cloud, отзовите скомпрометированный ключ и создайте новый — тогда вход должен пройти успешно.